export default function () {
    return (
        <div className="vopros">
            <h2>Вопрос по "use client"</h2>
            <p>Если в компоненте надо использовать какие либо хуки например useState то строго всегда надо указывать вначале кода "use client" ? (если выбрали app а не pages соответственно),
                возможно упустил на занятии может какие то другие подходы есть?</p>
        </div>
    );
}